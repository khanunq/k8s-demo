apiVersion: v1
kind: Service
metadata:
  name: demo-web-svc
spec:
  selector:
    app: demo-web
    color: blue   # Jenkins flips this to green later
  ports:
    - port: 80
      targetPort: 8080
  type: NodePort
